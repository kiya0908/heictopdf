# Cloudflare Pages 环境变量配置模板
# 复制此文件内容到 Cloudflare Pages 的环境变量设置中

# =============================================================================
# 必需的环境变量（必须配置）
# =============================================================================

# 数据库连接 - 使用 PostgreSQL
DATABASE_URL=postgresql://your_username:your_password@your_host:5432/your_database

# Clerk 认证配置
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key_here
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_publishable_key_here

# ConvertAPI 配置（用于 HEIC 转换）
CONVERTAPI_SECRET=your_convertapi_secret_key_here

# HashID 盐值（用于生成唯一ID）
HASHID_SALT=your_random_hash_salt_here

# =============================================================================
# 可选的环境变量（根据需要配置）
# =============================================================================

# Upstash Redis 缓存（推荐用于生产环境）
# UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your_redis_token_here

# AWS S3 存储配置（如果使用 S3 存储文件）
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_REGION=us-east-1
# S3_ACCESS_KEY=your_s3_access_key
# S3_SECRET_KEY=your_s3_secret_key
# S3_BUCKET=your_bucket_name
# S3_URL_BASE=https://your-bucket.s3.amazonaws.com

# 邮件服务配置（如果需要发送邮件）
# RESEND_API_KEY=re_your_resend_api_key_here
# NEXT_PUBLIC_SITE_EMAIL_FROM=noreply@yourdomain.com
# SITE_NOTIFICATION_EMAIL_TO=admin@yourdomain.com

# Stripe 支付配置（如果需要支付功能）
# STRIPE_API_KEY=sk_test_your_stripe_secret_key
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
# NEXT_PUBLIC_STRIPE_PRO_MONTHLY_PLAN_ID=price_your_plan_id
# NEXT_PUBLIC_STRIPE_PRO_YEARLY_PLAN_ID=price_your_plan_id

# PayPal 支付配置（临时禁用的功能）
# PAYPAL_CLIENT_ID=your_paypal_client_id
# PAYPAL_CLIENT_SECRET=your_paypal_client_secret
# NEXT_PUBLIC_PAYPAL_CLIENT_ID=your_paypal_client_id

# 监控和分析配置
# LOG_SNAG_TOKEN=your_logsnag_token
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_UMAMI_DATA_ID=your_umami_id

# Sentry 错误监控配置
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# =============================================================================
# Cloudflare 特定环境变量（自动设置，无需手动配置）
# =============================================================================

# CF_PAGES=1  # 自动设置
# CLOUDFLARE_ENV=production  # 自动设置

# =============================================================================
# 公开环境变量（前端可见）
# =============================================================================

# 网站 URL
NEXT_PUBLIC_APP_URL=https://your-domain.pages.dev

# =============================================================================
# 配置说明
# =============================================================================

# 1. 在 Cloudflare Pages Dashboard 中：
#    - 进入项目设置 > Environment variables
#    - 添加上述必需的环境变量

# 2. 数据库推荐：
#    - Supabase (推荐): https://supabase.com/
#    - PlanetScale: https://planetscale.com/
#    - Neon: https://neon.tech/

# 3. Redis 缓存推荐：
#    - Upstash Redis: https://upstash.com/

# 4. 文件存储推荐：
#    - AWS S3
#    - Cloudflare R2 (与 Pages 集成更好)

# 5. 部署后测试：
#    - 检查数据库连接
#    - 测试文件上传功能
#    - 验证认证流程